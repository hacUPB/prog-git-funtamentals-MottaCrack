# Crear Repositorio Local

Para crear un repositorio local hay que navegar usando el comando **cd** hasta la direccion en la que queremos crear el repositorio, usar el comando **mkdir** para crear un nuevo directorio dentro del cual usaremos el comando **git init** para iniciar el repositorio nuevo en ese directorio.